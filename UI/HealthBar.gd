extends TextureProgressBar

#var tween = create_tween()

func _ready():
	pass
	#value = get_parent().currentHP
	#max_value = get_parent().maxHP


func animate_hp_change(_new_hp: int, _old_hp: int):
	pass
	#tween.interpolate_value(old_hp, new_hp, 0.1, tween.TRANS_SINE, tween.EASE_OUT, 1)
	#tween.start()
